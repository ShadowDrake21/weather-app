export type Units = 'imperial' | 'metric' | 'standart';

export type ArrowsType = {
  leftArrow: boolean;
  rightArrow: boolean;
};

export type PagesProportions = {
  currentPage: number;
  allPages: number;
};
